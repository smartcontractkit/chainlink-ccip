# Generic overrides for CCIP staging deployments
---
in: |
  deployments:
    chainlink-don:
      helm:
        values:
          # this is a hack to make the ccip load test dashboard working for both CRIB and prod env
          # Dashboard uses a regex that requires pod name to have 4 parts
          # todo: this doesn't work unless we refactor ccip-v2-scripts implementation as well
          # fullnameOverride: cl-ccip-crib-simulated
