version: v2beta1
name: ccip-v2-scripts

vars:
  LOCAL_TEMPLATES_DIR:
    source: env
    default: ./templates
  TMP_DIR:
    source: env
    default: "${DEVSPACE_TMPDIR}/ccip-v2"
  CHAINS_COUNT:
    source: env
    default: 2
